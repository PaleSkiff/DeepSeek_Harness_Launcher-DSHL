<Window x:Class="DeepSeekHarnessLauncher.Views.PortOccupiedDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Dlg.PortTitle}" Width="460" Height="240"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" Background="#000000"
        ShowInTaskbar="False" FontFamily="Segoe UI">
    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="16"
            BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" Margin="1">
        <StackPanel Margin="24">
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,24"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{DynamicResource Btn.Cancel}" Width="80" Padding="0,7" Margin="0,0,8,0"
                        Style="{StaticResource SecondaryButtonStyle}" Click="Cancel_Click"/>
                <Button Content="{DynamicResource Btn.ViewProcess}" Width="120" Padding="0,7" Margin="0,0,8,0"
                        Style="{StaticResource SecondaryButtonStyle}" Click="ViewProcess_Click"/>
                <Button Content="{DynamicResource Btn.ChangePort}" Width="90" Padding="0,7" Click="ChangePort_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
