<UserControl x:Class="DeepSeekHarnessLauncher.Views.ServiceControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:DeepSeekHarnessLauncher.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:ServiceControlViewModel}"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,8,0">

            <!-- 状态大卡片 -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="18" Height="18" VerticalAlignment="Center"
                                 Fill="{Binding State, Converter={StaticResource StateToBrush}}"/>
                        <TextBlock Text="{Binding State, Converter={StaticResource StateToText}}"
                                   FontSize="22" FontWeight="Bold" Margin="10,0,0,0"
                                   VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <TextBlock Text="{Binding StatusPortText}" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Text="  ·  " Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding StatusPidText}" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Text="{DynamicResource Lbl.StartedAt}" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding StartedAtText}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                    <TextBlock Text="{Binding HealthText}" Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,6,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- 主操作区 -->
            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                <Button Content="{DynamicResource Btn.Start}" Command="{Binding StartCommand}" Width="110" Padding="0,9" FontSize="14"/>
                <Button Content="{DynamicResource Btn.Stop}" Command="{Binding StopCommand}" Width="110" Margin="12,0,0,0" Padding="0,9" FontSize="14"
                        Style="{StaticResource SecondaryButtonStyle}"/>
                <Button Content="{DynamicResource Btn.Restart}" Command="{Binding RestartCommand}" Width="110" Margin="12,0,0,0" Padding="0,9" FontSize="14"
                        Style="{StaticResource SecondaryButtonStyle}"/>
            </StackPanel>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="#C62828" TextWrapping="Wrap"
                       Margin="0,10,0,0"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVis}}"/>

            <TextBlock Text="{Binding EnvironmentMissingHint}" Foreground="#F59E0B" TextWrapping="Wrap"
                       Margin="0,10,0,0" FontWeight="SemiBold"
                       Visibility="{Binding EnvironmentMissingHint, Converter={StaticResource NullToVis}}"/>

            <!-- 健康检查 -->
            <GroupBox Header="{DynamicResource Group.HealthCheck}" Padding="10" Margin="0,16,0,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Lbl.Address}" Foreground="{StaticResource TextSecondaryBrush}" Width="80"/>
                        <TextBlock Text="{Binding StatusPortText}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="{DynamicResource Lbl.LastResult}" Foreground="{StaticResource TextSecondaryBrush}" Width="80"/>
                        <TextBlock Text="{Binding HealthText}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="{DynamicResource Lbl.LastCheck}" Foreground="{StaticResource TextSecondaryBrush}" Width="80"/>
                        <TextBlock Text="{Binding LastHealthText}"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- 环境摘要 -->
            <GroupBox Header="{DynamicResource Group.EnvironmentSummary}" Padding="10" Margin="0,16,0,0">
                <TextBlock Text="{Binding EnvironmentSummary}" TextWrapping="Wrap"/>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>
