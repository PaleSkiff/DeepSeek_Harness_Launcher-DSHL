<UserControl x:Class="DeepSeekHarnessLauncher.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:DeepSeekHarnessLauncher.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="{DynamicResource Btn.Save}" Width="80" Padding="0,6" Command="{Binding SaveCommand}"/>
                <Button Content="{DynamicResource Btn.Cancel}" Width="80" Margin="6,0,0,0" Padding="0,6" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,8,0">

                <GroupBox Header="{DynamicResource Group.Startup}" Padding="10" Margin="0,0,0,12">
                    <StackPanel>
                        <CheckBox Content="{DynamicResource Lbl.AutoStartOnBoot}" IsChecked="{Binding AutoStartOnBoot}" Margin="0,2"/>
                        <CheckBox Content="{DynamicResource Lbl.AutoStartService}" IsChecked="{Binding AutoStartServiceOnLaunch}" Margin="0,8,0,2"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{DynamicResource Group.CloseBehavior}" Padding="10" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Lbl.CloseMainWindow}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <RadioButton Content="{DynamicResource Lbl.MinimizeToTray}" GroupName="closeBehavior"
                                     IsChecked="{Binding CloseToTray}" Margin="0,2"/>
                        <RadioButton Content="{DynamicResource Lbl.ExitApp}" GroupName="closeBehavior"
                                     IsChecked="{Binding CloseToTray, Converter={StaticResource InverseBool}}" Margin="0,6,0,2"/>
                        <CheckBox Content="{DynamicResource Lbl.AskOnFirstClose}" IsChecked="{Binding AskOnFirstClose}" Margin="0,8,0,2"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{DynamicResource Group.Logging}" Padding="10" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Lbl.LogRetentionDays}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Width="100" Padding="2" Text="{Binding RetentionDaysText, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="{DynamicResource Lbl.RetentionHint}" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{DynamicResource Group.Service}" Padding="10" Margin="0,0,0,12">
                    <CheckBox Content="{DynamicResource Lbl.StopOnExit}" IsChecked="{Binding StopServiceOnExit}" Margin="0,2"/>
                </GroupBox>

                <GroupBox Header="{DynamicResource Group.Language}" Padding="10" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Lbl.Language}" VerticalAlignment="Center" Margin="0,0,8,0"
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                        <ComboBox SelectedValue="{Binding Language, Mode=TwoWay}" SelectedValuePath="Tag" Width="130">
                            <ComboBoxItem Content="中文" Tag="zh-CN"/>
                            <ComboBoxItem Content="English" Tag="en-US"/>
                        </ComboBox>
                    </StackPanel>
                </GroupBox>

                <TextBlock Text="{Binding ValidationError}" Foreground="#C62828" TextWrapping="Wrap"
                           Visibility="{Binding ValidationError, Converter={StaticResource NullToVis}}"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
